# CineForge Agent 本地部署执行计划

> **执行约束：** 服务仅在本机 `127.0.0.1:1241` 提供访问，并从当前源码构建。

1. 新建 Compose 本地覆盖文件，指定本地构建镜像与回环端口。
2. 由示例文件生成未跟踪的 `deploy/.env`，填入随机管理凭据与令牌。
3. 先运行 `docker compose config` 验证合并后的 Compose 配置。
4. 构建并后台启动服务。
5. 检查 Compose 服务状态、端口绑定和 `/health` 健康接口。
